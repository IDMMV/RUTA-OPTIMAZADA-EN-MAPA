# Rutas de Inspección V13.61

Reparación funcional de controles de mapa, carga directa de usuarios, apertura de formularios y optimización con respaldo local.

Conservar en `config.js` la URL real de Apps Script terminada en `/exec` y el Google Client ID real. Si se actualiza `apps-script.gs`, volver a implementar la aplicación web en Apps Script.
